# Database configuration (Defaults to SQLite for easy local setup, can be configured to PostgreSQL)
DATABASE_URL=sqlite:///./campus_governance.db

# JWT Security Configurations
SECRET_KEY="your_secret_key_here"  # Change this to a strong secret key in production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# File Storage
UPLOAD_DIR=uploads
